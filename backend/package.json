{
  "name": "trip-planner-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Tiny Express server that proxies trip-planning prompts to an LLM and forces structured JSON output via tool-use, so the API key never touches the browser.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.32.1",
    "@google/genai": "^2.12.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
